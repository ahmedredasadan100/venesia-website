import { VENESIA_SCROLLBAR_VISUAL_CLASSES } from "../../venesia-scrollbar-styles";

/** Compatibility alias for existing Admin adopters of the neutral owner. */
export const ADMIN_SCROLLBAR_VISUAL_CLASSES =
  VENESIA_SCROLLBAR_VISUAL_CLASSES;
